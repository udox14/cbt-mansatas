{
  "name": "cbt-pmb-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute pmb-man1-tasik --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute pmb-man1-tasik --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.5.0"
  }
}
